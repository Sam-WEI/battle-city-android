package com.samwdev.battlecity.utils

import android.util.Log

private const val TAG = "BattleCity"

fun logD(msg: String) {
    Log.d(TAG, msg)
}

fun logI(msg: String) {
    Log.i(TAG, msg)
}

fun logW(msg: String) {
    Log.w(TAG, msg)
}

fun logE(msg: String) {
    Log.e(TAG, msg)
}